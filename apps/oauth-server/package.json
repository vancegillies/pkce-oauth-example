{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev --port 3001",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "nitropack": "latest"
  }
}
